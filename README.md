# Mini-Project-1
Police Secure Check
Creating a project with python and sql with dashboard creation on streamlit
Create a file with extension ipynb for jupyter notebbok
import libraries-pandas,mysql connector, streamlit,plotly by giving pip install
Locating the file path and reading the file path.
Removing the NAN values from the Excel file.
Downloading the Cleaned data file.
Connecting to the MYSQL Server.
Creating a table and inserting the values into the Tbale.
Login to the dbeaver and check if the datbase and table is created successfully.
Writing thw SQL Queries for the allocated one.
For each query check if the iutput is coming in the dbeaver.
Open a file as .py extension.
import streamlit and plotly libraries by using pip install.
Connect to the MYSQL database.
Give a title for streamlit dashboard.
Prepre a log form with the required details.
Input the queries and the query mapping.
Run the query and get the output.



